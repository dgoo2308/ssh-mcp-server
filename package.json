{
  "name": "ssh-remote-commands-mcp",
  "version": "0.1.0",
  "description": "MCP server for SSH remote commands with SSH config integration and base64 encoding",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "ssh-remote-commands-mcp": "build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node build/index.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "ssh",
    "remote",
    "iot",
    "odoo",
    "base64"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "build/**/*"
  ]
}
